{ lib, ... }:
{
  # Placeholder hardware configuration for the desktop host.
  # Replace this file with the one generated on your machine via:
  #   nixos-generate-config
  # Then re-run your build with that file committed.
}
